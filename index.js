import { getRndInteger, getLastDigit, gen, random, verify, completeid, thaiIdRegex } from "./src"

export default gen;
export {
  getRndInteger,
  getLastDigit,
  gen,
  random,
  verify,
  completeid,
  thaiIdRegex,
};
